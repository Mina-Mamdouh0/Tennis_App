import 'package:flutter/material.dart';

class DialogProgressIndicator extends StatelessWidget {
  const DialogProgressIndicator({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: CircularProgressIndicator(),
    );
  }
}
